services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: market-data-gateway
    environment:
      LOG_LEVEL: INFO
      PORT: 8000
      HOST: 0.0.0.0
    ports:
      - "8000:8000"
    restart: unless-stopped
